
// Functions are used to  perform a task
function greet(name, lastName) {    // in the paranthese are parameters
console.log('Hello ' + name + ' ' + lastName);
}

// Calculating a valu
function square(number) {
    return number * number;
}

// We can create a variable that holds a value and pass that value to
// the function square
let inputValue = 2;
console.log(square(inputValue));